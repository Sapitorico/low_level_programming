#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int _putchar(char c);

char *create_array(unsigned int size, char c);

char *_memset(char *s, char b, unsigned int n);
#endif
